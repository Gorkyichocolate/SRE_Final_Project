groups:
  - name: app-alerts

    rules:
      - alert: HighCPUUsage

        expr: rate(process_cpu_seconds_total[1m]) > 0.8

        for: 1m

        labels:
          severity: warning

        annotations:
          summary: High CPU usage detected